请对最近的代码变更进行审查，严格按照以下步骤执行：

**第一步：收集信息（只读，不修改任何文件）**
- 运行 `git log --oneline -10` 查看最近提交
- 运行 `git diff HEAD~3..HEAD --stat` 了解变更范围
- 阅读涉及的关键文件

**第二步：输出审查报告**
按严重程度分类列出所有发现：
- 🔴 严重：可能导致运行时错误、数据丢失、安全漏洞
- 🟡 中等：逻辑问题、测试缺失、配置风险
- 🟢 低优先级：代码风格、可读性建议

**第三步：停下来等待用户确认**
报告输出完毕后，明确询问："是否需要我应用以上修复？" 不得在用户确认前自行编辑任何文件。

**第四步（仅在用户确认后）：应用修复**
- 按严重程度从高到低依次修复
- 每修复一个问题后在本地运行相关测试
- 全部修复完毕后运行完整测试套件，确认无连锁故障
- 最后提交并推送
